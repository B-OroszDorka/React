import { TextField } from "@mui/material";

function TextComponent({ label }) {
    return (
            <TextField
                label={label}
                margin="normal"
            />
    )
}

export default TextComponent;
